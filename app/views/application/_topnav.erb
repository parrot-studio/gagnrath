<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="<%= root_path %>"><strong><%= site_title%></strong> <%= site_sub_title%></a>
      <ul class="nav pull-right">
        <li><%= link_to('ご利用上の注意', notes_path) %></li>
        <% unless time_locked? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Strategy Room <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to('Timeline', timeline_path) %></li>
              <li><%= link_to('Result', result_path) %></li>
            </ul>
          </li>
        <% end %>
        <% if updatable_mode? %>
          <li><%= link_to('Admin Tools', admin_path) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
